# 변경 이력

버전 표기는 [유의적 버전](https://semver.org/lang/ko/)을 따릅니다.

---

## [0.1.0] - 2026-08-21

최초 공개.

### 기능
- **채널 블랙리스트**: 등록한 채널의 숏츠에 진입하면 자동으로 일시정지하고 화면 중앙에 안내를 표시합니다.
  화면을 클릭하면 그 영상에 한해 계속 볼 수 있습니다.
- **채널 추천 안 함**: 차단 안내의 버튼으로 유튜브의 `⋮ > 채널 추천 안 함`을 대신 실행합니다.
- **관리 페이지**: 팝업에는 최근 등록한 채널만 표시하고, 전체 목록은 별도 페이지에서
  검색·정렬·삭제할 수 있습니다. 정렬은 등록일 ↓↑, 이름 ↓↑ 네 가지입니다.
  팝업 표시 개수도 여기서 바꿉니다.
- **광고 자동 재생 막기(선택, 기본 꺼짐)**: 켜면 광고를 일시정지하고 안내를 표시합니다.
  클릭하면 재생됩니다.
- **한국어 / 영어 UI**: 브라우저 언어에 따라 자동 전환됩니다(`_locales/`).
  `ko`·`ko-KR` 은 한국어, 그 밖의 언어는 `default_locale` 인 영어로 폴백합니다.
  단 유튜브 메뉴 문구 매칭과 `aria-label` 탐색은 i18n 대상이 아닙니다.
  그쪽은 브라우저 언어가 아니라 유튜브 UI 언어를 따르므로 양쪽을 모두 들고 있어야 합니다.
- 한글·특수문자 채널명 지원. 채널 링크의 URL 인코딩을 디코딩해 판별합니다.
- 데이터는 `chrome.storage.local`에만 저장하며 외부로 전송하지 않습니다.

---

## 개발 노트

공개 전 개발 과정에서 확인한 사항입니다. 유튜브 DOM이 바뀌어 기능이 깨졌을 때
어디를 다시 봐야 하는지 판단하기 위한 기록입니다.

### YouTube DOM 관측 (2026-08 기준)

- **숏츠 광고는 오버레이가 아니라 reel 자체로 피드에 삽입된다.**
  `ytd-reel-video-renderer > ytd-ad-slot-renderer > ytd-in-feed-ad-layout-renderer`
- **광고 재생 중에도 `.html5-video-player`에 `ad-showing`이 붙지 않았다.** `ad-created`만 확인됐다.
  `ad-created`는 광고가 한 번 생성되면 남으므로 판정에 쓰면 안 된다.
- 광고 reel로 넘어갈 때 **URL이 바뀌지 않을 수 있다.** 그래서 항목 전환 감지는 URL과
  활성 reel 요소 교체를 함께 본다.
- `⋮` 버튼은 액션 바 안에 있고 구조는 다음과 같다. `aria-label` 은 **`추가 작업`**이다
  (`기타 작업`·`더보기`가 아니다). 라벨은 UI 언어를 타므로 구조 기반 셀렉터를 우선한다.

  ```
  ytd-menu-renderer.style-scope.ytd-shorts-player-controls
    > yt-button-shape#button-shape.style-scope.ytd-menu-renderer
      > button[aria-label="추가 작업"]
  ```

- `⋮` 메뉴 항목은 `yt-list-item-view-model`이고, **`채널 추천 안함` 바로 옆이 `신고`다.**
  부분 일치나 순서 기반 탐색은 오클릭 위험이 있어 문구 완전 일치만 사용한다.
- 메뉴 항목을 전역에서 훑으면 41개가 잡히고 그중 숏츠 메뉴는 8개뿐이다.
  나머지는 사이드바 가이드(`tp-yt-paper-item`, `role=link`)와 댓글 정렬(`role=option`)이다.
  숏츠 메뉴 항목은 `yt-list-item-view-model` / `role=listitem` 이고 클릭 대상은 내부 `button` 이다.
- 메뉴 드롭다운의 최상위는 `tp-yt-iron-dropdown` 이다. 페이지에는 닫힌 드롭다운(댓글 정렬 등)이
  여럿 떠 있으므로 **열려 있는 드롭다운 안에서만** 항목을 찾는다. 메뉴가 비동기로 렌더되는
  동안 전역으로 넓히면 아직 안 열린 우리 메뉴 대신 닫힌 메뉴의 항목을 집는다.
- 채널바는 `.ytReelChannelBarViewModelChannelName a`. 진입 직후에는 늦게 렌더된다.

### 설계 판단

- **광고 판정은 활성 reel 기준으로만 한다.** `ytd-ad-slot-renderer`를 document 전역에서 찾으면
  피드에 미리 로드된 광고 슬롯까지 걸려 차단이 통째로 무력화된다.
- **`video.pause()`는 유튜브 플레이어 상태를 오염시켜 다음 영상까지 정지가 이어진다.**
  그래서 확장이 재생을 막은 적이 있을 때만, 영상 전환 직후 짧은 창 안에서 1회 재생을 복구한다.
  이 창을 벗어나면 사용자가 직접 누른 일시정지는 절대 되돌리지 않는다.
- **활성 reel은 뷰포트를 절반 이상 덮는 것만 인정한다.** 화면 밖 이전 영상을 활성으로 오인하면
  직전 채널명을 읽어 엉뚱한 영상을 정지시킨다.
- 채널명 정규화(`@` 제거·소문자·trim) 규칙이 화면마다 어긋나 중복 등록이 생겼던 적이 있어
  `shared.js` 한곳에 모았다.
- 저장 형식은 `[{ name, addedAt }]`이며 구 문자열 배열을 읽을 때 자동 변환한다.
  변환 시점에 실제 등록일은 알 수 없으므로 `addedAt`을 지어내지 않고 `null`로 둔다.
